{
    "name": "@heekowave/shared",
    "version": "1.0.0",
    "description": "Shared types and utilities for Heekowave",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "type-check": "tsc --noEmit",
        "lint": "echo 'No linting configured'"
    },
    "dependencies": {
        "viem": "^2.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0"
    }
}